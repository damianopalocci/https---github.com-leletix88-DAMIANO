myapp.controller("dettaglio_lavorazioneController", [
    "$scope", "$rootScope", "$http", "$routeParams", "$location",

    function($scope, $rootScope, $http, $routeParams, $location) {
        // create a message 
        console.log('dettaglio_lavorazioneController');







    }
]);